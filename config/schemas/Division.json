{
    "$schema": "http://json-schema.org/draft-07/schema",
    "type": "object",
    "properties": {
      "Division": {
        "$ref": "#/definitions/Division"
      }
    },
    "required": ["Division"],
    "title": "Division",
    "definitions": {
      "Division": {
        "$id": "#/properties/Division",
        "type": "object",
        "title": "The Division Schema",
        "uniqueIndexFields": "DivisionCode",
        "required": ["name", "DivisionCode", "higherHierarchy"],
        "properties": {
          "divisionCode": {
            "type": "integer"
          },
          "name": {
            "type": "string"
          },
          "higherHierarchy": {
            "type": "string"
          }
        }
      }
    }
  }
  
  